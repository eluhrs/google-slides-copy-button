{
  "manifest_version": 3,
  "name": "Slides Prompt Copier",
  "version": "0.14",
  "description": "Adds a Copy button on Google Slides that copies the text after a configurable label (e.g. 'PROMPT:') on the current slide.",
  "permissions": ["clipboardWrite", "storage"],
  "content_scripts": [
    {
      "matches": ["https://docs.google.com/presentation/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
